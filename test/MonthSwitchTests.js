/**
 * Created by py on 24/07/16.
 */

var expect = require('chai').expect;
var MonthSwitch = require('../common/MonthSwitch');
var Shared = require('../common/Shared');

describe('MonthSwitch', function () {
    describe('#constructor', function(){
        // var state = Shared.getInstance().state;
        // var ms = new MonthSwitch(state);
        // it('should have Array months attached to it', function () {
        //     expect(ms.months.length).to.be.equal(6);
        // });
        // it('should have Month object as each element in this.months', function () {
        //     expect(ms.months[0]).to.have.property('timeWindow');
        //     expect(ms.months[0]).to.have.property('totals');
        //     expect(ms.months[0]).to.have.property('getUrl');
        // });
    });
});